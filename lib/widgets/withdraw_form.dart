import 'package:flutter/material.dart';

class WithdrawForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Withdraw Form"),),
    );
  }
}