import 'package:flutter/material.dart';

class ItemDetailEdit extends StatelessWidget {
  const ItemDetailEdit({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Edit"),
    );
  }
}