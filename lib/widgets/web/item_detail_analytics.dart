import 'package:flutter/material.dart';

class ItemDetailAnalytics extends StatelessWidget {
  const ItemDetailAnalytics({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Item Detail Analytics")
    );
  }
}