import 'package:flutter/material.dart';

class WebProfileScreen extends StatelessWidget {
  const WebProfileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Web Profile Screen"),
    );
  }
}
